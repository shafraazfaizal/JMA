// src/app/page.tsx — used ONLY on the launch-reveal branch
// Swap back to the full homepage after the gathering
import LaunchReveal from "@/components/launch-reveal/LaunchReveal";

export default function LaunchPage() {
  return <LaunchReveal />;
}